{
  "name": "cms-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix apps/admin-ui install && npm --prefix apps/admin-ui run build",
    "dev:api": "npm --prefix apps/api run dev",
    "dev:ui": "npm --prefix apps/ui run dev",
    "dev:admin": "npm --prefix apps/admin-ui run dev"
  }
}
